// Inicializar una variable con un valor.
var producto = "Monitor de 24 Pulgadas";

// Las variables se pueden reasignar.
producto = "Monitor de 19 Pulgadas";

// JavaScript es un lenguaje de tipado dinamico.
producto = 20;

// Se puede inicializar sin un valor y asignarlo después.
var disponible;
disponible = true;

// Inicializar multiples variables
var categoria = 'Computadoras',
    marca = 'Dell',
    calificacion = 5;

//Las variables no pueden iniciar con numeros.

// Estilos para crear variables.
// CamelCase. (más común en js).
var nombreProducto;

// SnakeCase o underscore
var nombre_case;

// Pascal Case (más útilizado en nombre de clases).
var Producto;

